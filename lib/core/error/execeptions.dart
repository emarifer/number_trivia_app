class ServerException implements Exception {}

class CacheException implements Exception {
  // Por si hiciera falta añadir en otros proyectos
  // final String message;

  // CacheException(this.message);
}
